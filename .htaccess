<IfModule mod_rewrite.c>
    RewriteEngine On

    # Không rewrite file / folder thật
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Mọi request đều đẩy vào public/index.php
    RewriteRule ^(.*)$ public/index.php?url=$1 [QSA,L]
</IfModule>
