<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /MVC_G1-LIBRARY-MANAGE-SYSTEM
    # Đưa mọi yêu cầu vào thư mục public
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>
